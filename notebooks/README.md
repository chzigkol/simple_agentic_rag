# Notebooks

Use this folder for notebook workflows that import the production package:

```python
from agentic_rag.cli import build_pipeline
from agentic_rag.settings import Settings
```

After installing the project with `uv sync --extra dev`, notebooks can use the
project environment as their kernel.
